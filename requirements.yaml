packages:
  "framer-motion": "For complex page transitions and sunrise animations"
  "date-fns": "For formatting dates and handling time-appropriate greetings"
  "clsx": "For conditional class merging"
  "tailwind-merge": "For conditional class merging"

images: []

notes:
  - App relies on token-based auth stored in localStorage ('falaq_token')
  - Uses framer-motion for smooth PWA-like page transitions and interactive elements
  - PWA mobile-first design: desktop view constrained to max-w-md to simulate mobile app
